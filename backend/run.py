"""
Startup script for TryOnAI Backend
Runs uvicorn server with proper configuration
"""
import uvicorn

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
        log_level="info"
    )
